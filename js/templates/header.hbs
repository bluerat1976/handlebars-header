<div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        {{#each header.navItems}}
        <li class="nav-item {{this.customClass}}">
            <a class="nav-link" href="{{this.href}}">{{this.label}}</a>
        </li>
        {{/each}}
    </ul>
</div>